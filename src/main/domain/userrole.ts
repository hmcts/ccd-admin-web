export class UserRole {

    public role: string;
    public classification: string;

    constructor(role: string, classification: string) {
        this.role = role;
        this.classification = classification;
    }
}
