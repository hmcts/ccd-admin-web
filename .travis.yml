language: node_js
node_js:
  - "8.4"
cache: yarn
script:
  - yarn --version
  - yarn sass
  - yarn check || echo 'yarn check failed'
  - yarn lint
  - yarn test
  - yarn test:routes
  - yarn test:a11y
  - yarn test:nsp
