# Snyk (https://snyk.io) policy file, patches or ignores known vulnerabilities.
version: v1.12.0
ignore: {}
# The historical tunnel-agent patch is no longer required: the vulnerable
# @hmcts/nodejs-logging > log4js > request dependency chain is absent from yarn.lock.
# The remaining audit finding is recorded in yarn-audit-known-issues.
# patches apply the minimum changes required to fix a vulnerability
patches: {}
